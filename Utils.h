#pragma once

#include "Container.h"

/*
    Description:
        - Returns a deep copy of a container.
    Inputs:
        - container: The container of which a deep copy should be made.
    Outputs:
        - A deep copy of the given container.
*/
Container* Utils_getDeepCopyOfFileContainer(Container* container);